<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Keith Pitty Admin" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Note: code removed for other icons until they are available -->

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Keith Pitty Admin</a>
          <% if current_user %>
            <div class="nav-collapse">
              <ul class="nav">
                <%= navbar_item "Home", "/admin" %>
                <%= navbar_item "Achievements", "/admin/achievements" %>
                <%= navbar_item "Testimonials", "/admin/testimonials" %>
                <%= navbar_item "Blog posts", "/admin/blog_posts" %>
                <%= navbar_item "Comments", "/admin/comments" %>
                <%= navbar_item "Images", "/admin/images" %>
              </ul>
            </div><!--/.nav-collapse -->
          <% end %>
        </div>
      </div>
    </div>

    <div class="container">

      <%= render "shared/alerts" %>

      <div class="row">
        <div class="span9">
          <%= yield :layout %>
        </div>
        <div class="span3">
          <% if current_user %>
            <%= yield :secondary_content %>
            <p><%= link_to 'Log out', '/logout' %></p>
          <% end %>
        </div>
      </div><!--/row-->
    </div> <!-- /container -->

    <div class="navbar navbar-fixed-bottom">
      <div class="navbar-inner">
        <div class="container">
          <footer>
            <p>&copy; <%= Time.zone.now.year.to_s %> Keith Pitty, all rights reserved.</p>
          </footer>
        </div>
      </div>
    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
