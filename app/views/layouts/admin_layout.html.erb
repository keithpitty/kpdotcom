<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Keith Pitty Admin" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_pack_tag "application", "data-turbolinks-track": "reload" %>
    <%= javascript_pack_tag "application", "data-turbolinks-track": "reload" %>

    <!-- Note: code removed for other icons until they are available -->

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>
    <div class="w-screen bg-blue-900">
      <div class="container mx-auto bg-blue-900 px-12 py-4">
        <nav class="flex items-center justify-between flex-wrap">
          <div class="flex items-center flex-shrink-0 text-white mr-6">
            <a href="#" class="font-thin text-xl tracking-tight text-blue-200">Keith Pitty Admin</a>
          </div>
          <div class="block lg:hidden">
            <button id="nav-toggle" class="flex items-center px-3 py-2 border rounded text-blue-200 border-blue-200 hover:text-white hover:border-white lg:hidden">
              <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
            </button>
          </div>
          <% if current_user %>
            <ul class="list-reset lg:flex justify-end flex-1 items-center">
              <li><%= navbar_item "Home", "/admin" %></li>
              <li><%= navbar_item "Achievements", "/admin/achievements" %></li>
              <li><%= navbar_item "Testimonials", "/admin/testimonials" %></li>
              <li><%= navbar_item "Blog posts", "/admin/blog_posts" %></li>
              <li><%= navbar_item "Comments", "/admin/comments" %></li>
              <li><%= navbar_item "Images", "/admin/images" %></li>
              <li><%= navbar_item 'Log out', '/logout' %></li>
            </ul>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container mx-auto px-12 py-6">
      <%= render "shared/alerts" %>
      <%= yield :layout %>
    </div> 

    <div class="container mx-auto px-12 pb-6">
      <p>&copy; <%= Time.zone.now.year.to_s %> Keith Pitty, all rights reserved.</p>
    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
